[package]
name = "orbit"
version = "0.1.0"
edition = "2024"

[dependencies]
# 内部依赖
orbit-core = { path = "../orbit-core" }

# 工作区依赖
tokio.workspace = true
clap.workspace = true
anyhow.workspace = true

